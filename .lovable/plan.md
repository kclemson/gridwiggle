
# Replace README Screenshot

Copy the uploaded image to `public/og-image.png` (overwriting the existing screenshot) so the README reference `./public/og-image.png` picks it up automatically. No other files need to change.

## Steps

1. Copy `user-uploads://image-588.png` to `public/og-image.png`
