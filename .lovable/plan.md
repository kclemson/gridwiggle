

# Bigger Thumbnails and Stronger Animation

Two small tweaks in `src/components/SampleGallery.tsx`:

1. **Double the thumbnail height**: Change `h-20` to `h-40` on the button elements so they're twice as tall and more visible.

2. **Increase scale animation to 25%**: Change `scale-110` to `scale-125` on the featured thumbnail so the pop effect is more pronounced.

## Technical Details

### File: `src/components/SampleGallery.tsx`

- Line 54: Change `h-20` to `h-40`
- Line 56: Change `scale-110` to `scale-125`

No other files change.

